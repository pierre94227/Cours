{%  extends "base.html.twig" %}

{% block body %}

	<div>
		<div id="profilsHead" class='d-flex flex-row align-items-center justify-content-between container border mt-3 bg-dark text-light col-11'>
			<h1>Page du Poste: {{ role.NomRole }}</h1>
			<a href="?page=ajoutProfil" class="button bg-light text-dark pr-2 pl-2">
				Ajout Role</a>
		</div>
		<div class="d-flex flex-row flex-wrap">

			<div class='col-11 container border d-flex flex-column justify-content-between align-items-center bg-light '>
				<p class="mt-5 mb-5 pb-5">Description du poste : {{ role.NomRole }} </p>
			</div>
		</div>
	</div>
{% endblock %}
